#include <stdio.h>
#include <time.h>
/**
 * main - Printfs if the number is positive, negative or 0
 * Return: Always 0
 */
int main(void)
{
int n;

rand(time(0));
n = rand() - RAND_MAX / 2;
/* your code goes there */
return (0);
}
